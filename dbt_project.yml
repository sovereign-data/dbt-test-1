name: 'dbt_test_1'
version: '1.0.0'

profile: 'trino'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  dbt_test_1:
    +schema: raw

models:
  dbt_test_1:
    silver:
      +schema: silver
      +materialized: table
    gold:
      +schema: gold
      +materialized: table
